import TopNavBar from "./component/header/TopNavBar";

export default function Home() {

  return (
    <div className="font-sans mt-0 mx-auto grid grid-rows-[20px_1fr_20px] items-start min-h-screen pb-20">
      <main className="flex flex-col ">
      {/* {cars.map((car, index) => (
        <h1 key={index}>This is {car} car.</h1>
      ))} */}
      </main>
      <footer></footer>
    </div>
  );
}
